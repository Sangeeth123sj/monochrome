<nav class="navbar sticky-top navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Monochrome", root_path, class: 'navbar-brand', style: 'font-size:30px;' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if signed_in? %>
          <li class="nav-item">
            <%= link_to 'Write an article', new_article_path, class: 'nav-link active' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign out', destroy_blogger_session_path, method: :delete, class: 'nav-link active' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Sign in', new_blogger_session_path, class: 'nav-link active' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>